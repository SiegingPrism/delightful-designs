# Flows

Mermaid-rendered flow diagrams. Mermaid renders natively on GitHub — no PNG export needed. If you want PNGs for a pitch deck, paste each block into [mermaid.live](https://mermaid.live) and export.

The full flow set lives in [`../product/app_flow.md`](../product/app_flow.md). Quick links below.

## Onboarding flow
See `product/app_flow.md §1`. ≤ 60 seconds, ≤ 5 taps, lands on a Dashboard with the Coach pre-loaded.

## Daily core loop
See `product/app_flow.md §2`. Open → score → coach → task → focus → done → habit → repeat.

## Task creation flow
See `product/app_flow.md §3`. Quick capture with optional AI prefill.

## Gamification loop
See `product/app_flow.md §4`. Action → XP → level check → streak → score → feedback.

## AI suggestion loop
See `product/app_flow.md §5`. Includes accept/snooze feedback into the next prompt.

## Burnout detection (Tier 2)
See `product/app_flow.md §6`. Triggers a Recovery Mission with 2× XP.
